\usepackage{geometry}
\usepackage{paracol}

% PAGE %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\geometry{
    paper=a4paper, % paper size
    top=1cm, % top margin
    bottom=1cm, % bottom margin
    left=1mm, % left margin
    right=1cm, % right margin
    headheight=0cm, % header height
    footskip=0cm, % space from the bottom margin to the baseline of the footer
    headsep=0cm, % space from the top margin to the baseline of the header
    %showframe, % show how the type blocks are set on the page
}

% COLUMNS %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\columnratio{0.34,0.66}

\setlength{\columnsep}{1em}
\setlength{\columnseprule}{0pt}
\colseprulecolor{rule}

\newcommand{\leftcol}{\switchcolumn[0]} % star for top alignment
\newcommand{\rightcol}{\switchcolumn[1]}

% PARAGRAPHS %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
